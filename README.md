# weblog
簡単な計算メモや公式を他人と共有したい場合に備えて公開.
github pagesを利用して簡単に閲覧可能にする.

新しく環境作った時用メモ
1. `git config user.name "fjmk"`
2. `git config user.email "匿名アドレス"`
3. `git remote add public https://github.com/k
aitheorychem/weblog.git`
4. `git push public main`
5. `git remote remove origin`